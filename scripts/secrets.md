## Secret data needed

No secret files are needed anymore — everything is environment variables now
(see the Environment Variables section in the repo readme).

`user-aliases.json` and `usermap.json` were used by the quote scripts and the
Clerk user linking; the quote scripts moved to the Riksdagen-Bot repo and Clerk
has been replaced with Discord OAuth.
